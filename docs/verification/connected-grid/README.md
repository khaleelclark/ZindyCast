# Connected translucent layout — September 12, 2026

Lead changed index.tsx and added connected-grid.css. One translucent forecast surface with shared horizontal/vertical dividers replaces separated Today cards. Current remains primary with the independently sized radar secondary. Mobile retains Current → Hourly → Daily → Radar. Search, geolocation and saved-place action form one toolbar with no empty background bar. Main tabs, unit controls, disclosure headings and horizon controls use consistent geometry and restrained colors. Weather background work is separately owned in weather-appearance/weather-scene.

New tests/browser/connected-grid.mjs reuses retained weather and map fixtures, blocks all external requests, and runs at1440×1000,1100×900,390×844. Final pass: no page overflow, transparent location wrapper, heat disclosure does not change map dimensions or add frame requests, unit toggles, save/remove roundtrip, search and geolocation-denied handling; no uncaught errors. Screenshot outputs visually reviewed. First test attempt expected Save place despite fixture already saved; corrected fixture action order and reran successfully. Production-like isolated build and typecheck passed;14 dashboard/weather node:test tests pass. Existing MapLibre build warning remains. No live Mapbox/NOAA/IEM/production counter calls. Final scene integration and published build pending.

Reproduce: PUBLIC_MAPBOX_ACCESS_TOKEN=' ' npm run build --workspace @zindycast/web -- --distPath /tmp/zindycast-connected-dist; node tests/browser/connected-grid.mjs. The space intentionally disables Mapbox without dotenv rehydrating the user's key.

Final integrated release: root typecheck/build and18 weather/dashboard tests pass. Browser layout/control/disclosure checks rerun against final apps/web/dist at1440/1100/390: pass. Animation scenarios independently verify cloud/sun/moon/star/rain/snow/fog, no duplicate pseudo animation, pause, hidden pause, reduced motion and offline neutral with no page errors. All external/API requests intercepted; no live provider or budget usage. Native SVG animation needs no image assets or new dependencies. Published with existing build flow; no service restart. Retained final logs and scene screenshots; reproducible scene harness added at tests/browser/weather-scene.mjs. Physical-device acceptance not claimed.
