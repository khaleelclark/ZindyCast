# Weather-aware appearance — September 11, 2026

Original inline SVG weather icons replace emoji in current/hourly forecasts and add daily condition icons. WMO mapping follows https://open-meteo.com/en/docs#weather_variable_documentation (accessed September11): clear/partly cloudy/daylight, overcast, fog, drizzle, rain, freezing rain, snow, thunderstorm, hail. Unknown codes retain a neutral icon. Daily icon follows the same condition selection as its textual summary, not an arbitrary first hour.

Decorative CSS sky gradients and current-card tints follow the eligible current modeled weather: sunny/night/cloudy/rain/snow/storm/fog. Clear/partly-cloudy uses supplied isDay, not the browser clock or inferred latitude. Missing/expired current hour, stale/aged/error forecast and offline use neutral theme. No new provider calls, imagery assets, location permissions, animation or weather inference. Cards and chart surfaces remain readable. These are illustrative themes, not observed sky photographs.

16 focused weather/appearance/dashboard tests pass; typecheck and isolated build pass (existing MapLibre warning). Actual fixture Chrome390×844 and1280×900 passes all seven themes, icons, no page horizontal overflow, offline reset and zero page exceptions/live provider calls. Screenshots inspected. Fixture intentionally varies weather independent of clock. Script now reads the served apps/web/dist release. Final integrated checks passed after icon row-height and tab-spacing refinements; see ../mui-weather-release/README.md. No physical-device or formal contrast certification claimed.
