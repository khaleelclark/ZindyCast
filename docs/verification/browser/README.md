# Browser audit, September 11, 2026

User reported broken maps and excessive reading. Actual installed Chrome152.0.7977.75 browser sessions used localhost4311; tailnet hostname currently fails DNS on the deployment host. Native Playwright1.63.0 is exact-pinned for repeatable browser interactions and screenshots; existing Google Chrome executable used, no browser binary downloaded. Source guidance: https://playwright.dev/docs/test-configuration and https://playwright.dev/docs/api/class-browsertype .

Reproduce manually against running preview: `node tests/browser/core-audit.mjs`, `node tests/browser/failure-audit.mjs`, `node tests/browser/pwa-audit.mjs`. These live suites make bounded real provider requests via application cache/budgets and create only isolated browser preferences. They are not included in ordinary npm test. Failure audit explicitly injects two error responses into its own browser; successful weather remains from live API. Initial inspect.mjs saves observed map/search screenshots.

core-results.json:11 grouped checks pass, no browser page errors. Search/forecast, saves/units/7/10/14/activity, hourly chart/table, reload preferences, granted geolocation, radar previous/current image, GOES,390px viewport overflow, offline/reconnect, remove save, actual service worker registration. First run selected hidden Compare h1 rather than visible heading, a test-selector issue corrected before successful rerun.

failure-results.json:5 checks pass: denied geolocation fallback, isolated forecast error/retry, isolated radar error/retry, Alaska radar/global selection, Hawaii radar. pwa-results.json:actual controlled page, offline shell reload preserving units, no API cache entries, online reload recovery pass.

Visually inspected map-before.png and alaska-global.png: PNG weather content actually renders. The current geographic context is poor: no boundaries/place labels, static regional image, no useful pan/zoom map. This does NOT close the user's radar complaint. Mobile screenshot shows excessive NWS area/time metadata above feels-like; sent concrete evidence to web owner to collapse details. Readability changes still need final browser verification after handoff. No blanket all-functionality acceptance.

Remaining browser checks include History/Compare/export flows (delegated parallel actual-browser audit), wildfire pending route rollout, multi-release/multi-tab updates, installed PWA/device permission flows, physical iPhone/Android/Safari, and geographic radar alignment/basemap. Exact image DOM availability does not prove numerical alignment or coverage. SVG/raster geographic limitations remain documented.

MapLibre6.9.0 dependency prepared for the required real map framework following official WMS/source docs and OSM interactive-tile policy. No MapLibre integration or basemap functionality is claimed yet. Sources: https://maplibre.org/maplibre-gl-js/docs/examples/add-a-wms-source/ ; https://maplibre.org/maplibre-style-spec/sources/ ; https://operations.osmfoundation.org/policies/tiles/ . No framework migration to MUI undertaken; user's word 'mud' remains awaiting clarification.

Follow-up: History download tested with actual Playwright download event/saveAs. Austin2020-07-15 loaded, form changed toJuly16, download preserved displayedJuly15 selection, ERA5 and SI temperature_C columns. File history-download.csv and metadata history-download.json. Initial test expected camel-case temperatureC rather than actual CSV temperature_C; test corrected, not product logic. Readability screenshots reviewed; real map framework/projection work and complete Compare lifecycle/browser export audit are tracked next.

## Integrated interactive map acceptance

`interactive-map-audit.mjs` now verifies real EPSG3857 frames over OSM in installed Chrome, waits for actual source load plus animation frames, and records screenshots/response projections. Nine grouped checks pass: initial rendered overlay; zoom viewport; drag-pan viewport; opacity; previous frame; Alaska radar/global infrared; Hawaii radar; offline removal/reconnect restoration;390px no horizontal overflow. No page/console errors in accepted run. Cancelled pending tile requests during region/offline changes are expected aborts, not missing-frame success. Visually inspected interactive-radar.png confirms colored radar echoes over labeled geographic context; last-map-source.png retains a sampled actual source image. Precise sensor/location accuracy and coverage masks are not established by visual projection alignment.

Lead fixed production packaging: explicit same-origin MapLibre6.9 worker URL and copied worker/shared module/license through Rsbuild output.copy. Previously default runtime worker path was unavailable. Added favicon link, completion tracking and zoom/resize viewport notifications. Initial tests that waited for global networkidle were unreliable with asynchronous map resources; accepted test waits for source readiness and two render frames. Regional selector exact-label test was corrected to select actual map controls before accepted run. MapLibre optional dynamic-import build warning remains; required worker and GeoJSON footprint now render successfully in tested Chrome. No suppression of unknown coverage or source time.

Comparison audit passed eight real browser lifecycle/export checks; see ../browser-comparison.md and its saved downloads. History CSV bytes were separately verified. Core/PWA previous evidence remains scoped to named checks, not every physical device or undeveloped feature. Maplibre6.9.0 worker/shared files add approximately530kB uncompressed; map remains lazy-loaded. PWA does not offline-cache tiles/weather/vendor worker.

## Final current-build checks

After the interactive map changes, core-results.json was regenerated: all 11 groups pass with no page errors. failure-results.json was also regenerated: all five groups pass. These supersede the earlier static-map findings above; those paragraphs retain investigation history.

`node tests/browser/wildfire-audit.mjs` exercised the explicit Load wildfire evidence button in actual Chrome. HTTP200 returned 17 incidents and two perimeters for the Roseville-area bbox; the browser asserted the no-evacuation wording. Evidence: wildfire-results.json. This verifies the evidence list, not polygon rendering or coverage. Final local health endpoint returned status ok. Typecheck/build and 206 unit/integration tests passed for this release.

Still unverified: physical devices, installed iOS/Android behavior, touch gestures, full accessibility, multi-tab/multi-release update lifecycle, and spreadsheet-application import. Existing unavailable product features remain unavailable; no blanket claim that the complete implementation backlog works.
