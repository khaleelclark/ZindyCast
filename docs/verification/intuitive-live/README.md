# Intuitive redesign integration — September 11, 2026

Deployed larger-text homepage, default labeled 24-hour heat trend, direct Radar/Satellite/Future radar choices, single observed Play/Pause with bounded cached playback, and CONUS HRRR forecast timeline. Radar map occupies the first desktop column pair alongside current conditions. Source and warning semantics remain available; no invented future frames or heat categories.

Typecheck/build passed. Full suite: 277 passed, two opt-in fixture browser tests skipped (279 total). Specialists separately ran both opt-in actual Chrome fixture tests successfully. Static serving negotiates compressed public assets; eligible public artifact bytes 2,437,713 -> 632,181 Brotli. This is size reduction, not measured page-load-time improvement. Both private API/worker services restarted together.

Live Chrome test tests/browser/intuitive-live.mjs: Austin observed raster reached ready; no map dropdowns; Play control present. Future layer requested four selected-visible XYZ tiles, all HTTP200 with matching forecast validity 2026-09-11T16:30Z from pinned14:00Z run. Browser audit16:27Z, so these were genuinely future valid times. Screenshot inspected; little/no colored echo in this viewport is not proof of dry conditions or coverage. No page exceptions. 1280px/390px viewport widths had no horizontal overflow and16px base body. Later resize tile requests were locally blocked to avoid additional provider load. The screenshot and header evidence are live; no synthetic weather was injected.

Future timeline is manual, up to three hours in fifteen-minute steps, CONUS only. Only visible selected-frame tiles load; automatic future animation remains unavailable to avoid overwhelming the free source. Observed playback prepares at most six frames and replays cached images. Source spatial accuracy, physical phones/Safari and actual family comprehension are not certified by this browser run. Missing quantitative matching forecast palette remains explicitly illustrative.
