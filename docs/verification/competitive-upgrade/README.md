# Competitive upgrade — September 14, 2026

Implements the authorized competitive review within the existing private, free-service deployment. See `docs/decisions/competitive-upgrade.md` for scientific and notification boundaries.

Delivered: compact Today hierarchy, shared metric formatting, clear feels-like and hourly-rain labels, comparison presets, nearby NWS observations with explicit station choice, NOAA 1991–2020 station monthly normals, opted-in prospective forecast verification with best-match/GFS/IFS candidates, and device-opt-in official warning notifications.

Verification: production root build and TypeScript checks pass. Full suite after initial integration: 443 passed, five optional browser tests skipped, zero failures (448 total). Follow-up verification safeguards have focused evidence in their tests. Actual Chrome 390×844 and 1440×1000 fixtures passed keyboard location switching/details, unit changes, independent forecast outage, reduced motion, Compare presets/legacy disclosure and new panel mounting. Summary begins at 652px and upcoming hours at 827px on the phone fixture. Separate interactive panels pass observation station choice, 32°F conversion, normals load, accuracy start/stop, and notification enable/disable; four explicit fixture POSTs per width, zero external requests/registrations/page errors/overflow. These are fixture browser results, not physical-device or weather accuracy evidence.

Final visual inspection found a nighttime city-heading contrast defect; the new selected-place header and footer links now reuse the light nighttime foreground. A missing generated production entry was caught by shell tests; rebuilt assets restore the served page. Contract imports must follow the browser-resolvable extensionless convention. Root build passes with existing MapLibre optional dynamic-dependency warnings.

Verification safeguards: station metadata validated server-side under the shared NWS quota; target identity normalized while original provider request identity is retained; rain event means strictly more than 0.1mm in the exact preceding forecast hour; observation outages cannot prevent new forecast capture; inactive installations lose collection authority; SQL deduplicates before bounded scorecard materialization (50,000 total rows, explicit overflow). Baseline remains unchanged; future-only archives report pending. No inferred issue time, model winner, correction or confidence percentage.

Release checks and final service state are appended after activation. Local VAPID generation preserves private configuration and does not subscribe a device or send a message. Physical iPhone/Android/Safari notification delivery, seasonal/held-out accuracy evidence, calibrated corrections/ranges, verified minute rain timing, usability participants and public hosting/data licensing remain unverified or evidence-dependent. Do not describe these as delivered superiority.

Release completed September 14 at 15:35 UTC: local notification keys generated privately, the three pre-existing databases snapshotted while both services were stopped, and the updated API/worker started successfully. New notifications/verification databases were initialized by the release. No registrations, subscriptions or notification sends were created by this acceptance check. API and worker active with zero restarts; first maintenance successful. Local HTML/service worker return 200 with no-cache; health is ok; notification status is configured=true, enabled=false. Final post-fetch station freshness regression and related checks: 23/23 pass; shell/update/push-event tests: 12/12 pass. No physical push-delivery claim.

Private HTTPS `/api/v1/health` returns 200/ok after restart. Unauthenticated verification listing returns 401; invalid normals selection returns 400 without a provider request. A final private source/build snapshot and SHA-256 manifest accompany the stopped backups. Operational screenshots/browser artifacts use only fixtures. Warning-summary text within the light reading surface retains dark text at night, while city header/footer use the light sky foreground.
