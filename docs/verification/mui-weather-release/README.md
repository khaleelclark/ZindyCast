# MUI charts, heat guidance and weather appearance — September 11, 2026

Published combined frontend release. Both temperature/WBGT graphs use MUI X Community LineChart9.13.0, responsive to mobile width, precise displayed values, local-time/DST labels, null gaps and keyboard/touch access. Heat precautions reproduces NWS Tulsa's source-attributed break suggestions and preparation advice in one expandable section, highlights only unambiguous current reference bands, and retains no-risk-free/safe-time distinctions. Weather icons and decorative themes use actual WMO/isDay fields and neutral stale/offline fallback. No new backend/provider calls or service changes.

Typecheck and production build passed; 80 web tests passed, 2 opt-in map tests skipped. Existing MapLibre optional dynamic import warning persists. Actual fixture Chrome390×844/1280×844 against the combined served build passed hourly hover/tap/keyboard, exact F/C values, DST, missing values, tooltip bounds, touch page scroll, heat marker/graph, guidance expansion/rows, offline suppression and no horizontal overflow. Separate weather fixture checks passed seven themes at390/1280 with no page exceptions. Zero live provider calls; fixture results do not establish meteorological accuracy or physical-device/screen-reader acceptance.

Reproduce: node tests/browser/hourly-interaction.mjs; node tests/browser/heat-interaction.mjs; node docs/verification/weather-appearance/browser.mjs. All intercept APIs/assets/external tiles and use isolated browser preferences. Final range-label nowrap CSS refinement rechecked with heat browser script.

Official references: https://mui.com/x/react-charts/quickstart/ ; https://www.weather.gov/tsa/wbgt ; https://open-meteo.com/en/docs . Numerical WBGT policy remains unchanged. MUI license served at /notices/mui-x-charts.txt. Final aggregate build approximately2.77MB uncompressed/783kB gzip including map chunks; public asset Brotli approximately730kB. This adds about91kB gzip versus prior release, not a measured runtime-speed improvement. Existing PWA Update and reload installs new release while preserving preferences.
