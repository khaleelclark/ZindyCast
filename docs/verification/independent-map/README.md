# Independent current-card and map sizing — September 11, 2026

The desktop map previously stretched to the grid row height, so expanding the current card changed the geographic viewport even without a React lifecycle bug. Set the map wrapper to align-start and give the radar viewport its own bounded viewport-relative height (360–620px at55vh). Map controls stay in normal flow and the card can grow for its own content. Existing mobile viewport sizing and wider-current column ratio are retained. No map component/network/source code changed.

Browser regression exercises four Heat precautions open/close toggles, units change,20 chart hover samples and61s clock advancement per viewport (1440/1100/390). It asserts unchanged radar pixel dimensions, same canvas, no readiness transitions or extra frame requests, no page overflow/errors. All API/external requests are fixture-intercepted, zero live provider calls. Results stored after successful execution in results.json. Typecheck and production build passed; existing MapLibre warning remains. No claim that map never React-renders: legitimate time/location/layer/viewport updates remain functional. This change prevents neighboring content from resizing/reloading it.
