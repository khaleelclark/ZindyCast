# Optional browser-visible Mapbox token. Use public styles:tiles permission only.
# Restrict to the ZindyCast HTTPS origin (including its port).
# Put the real value in ignored apps/web/.env.local, then rebuild.
# Empty keeps the standard OSM map and sends no Mapbox requests.
PUBLIC_MAPBOX_ACCESS_TOKEN=
